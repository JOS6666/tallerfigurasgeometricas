import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.tallerfigurasgeometricas',
  appName: 'tallerfigurasgeometricas',
  webDir: 'www'
};

export default config;
